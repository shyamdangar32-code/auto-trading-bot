# Core data/TA
pandas>=2.2.2
numpy<2                    # hard cap - avoids NumPy 2.x issues
ta==0.11.0                 # we use 'ta' lib; not pandas-ta

# Broker & auth (Zerodha)
kiteconnect==5.0.1
pyotp>=2.9.0
requests>=2.32.3
PyYAML>=6.0.1

# Time & calendars
python-dateutil>=2.8.2
pytz>=2020.1

# Logging & cache
loguru>=0.7.2
cachetools>=5.3.3

# Charts (reports)
matplotlib>=3.8

# Tests (optional)
pytest>=7.4.4

# Tables (for DataFrame.to_markdown)
tabulate>=0.9.0

# ---- Optional (leave commented if unused) ----
# yfinance==0.2.43
