# Core data/TA
pandas>=2.2.2
numpy>=2.0
ta==0.11.0
pandas-ta>=0.3.14b # extra indicators (optional but handy)

# Broker & auth (Zerodha)
kiteconnect==5.0.1
pyotp>=2.9.0
requests>=2.32.3
PyYAML>=6.0.1

# Time & calendars (intraday trading windows)
python-dateutil>=2.8.2
pytz>=2020.1
tzdata>=2022.1
pandas-market-calendars>=4.4.1   # NSE/NFO trading sessions

# Options math (pricing & Greeks)
scipy>=1.11.0           # Black–Scholes, Greeks
numpy-financial>=1.0.0  # IRR/NPV helpers (optional for risk)

# Logging/ops
loguru>=0.7.2
cachetools>=5.3.3

# Charts (reports)
matplotlib>=3.8

# Tests (optional)
pytest>=7.4.4

# ---- Optional (leave if you still want Yahoo anywhere) ----
# yfinance==0.2.43
