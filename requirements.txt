# Core data/TA
pandas>=2.2.2
numpy>=1.24,<2.0        # NumPy 2.x એવોઇડ for TA stacks
ta==0.11.0
pandas-ta==0.3.14b0     # correct beta tag; wheels exist

# Broker & auth (Zerodha)
kiteconnect==5.0.1
pyotp>=2.9.0
requests>=2.32.3
PyYAML>=6.0.1

# Time & calendars (intraday trading windows)
python-dateutil>=2.8.2
pytz>=2020.1

# Logging & cache
loguru>=0.7.2
cachetools>=5.3.3

# Charts (reports)
matplotlib>=3.8

# Tests (optional)
pytest>=7.4.4

# Tables (needed for Pandas .to_markdown)
tabulate>=0.9.0

# ---- Optional (leave commented if unused) ----
# yfinance==0.2.43
